import { displayError } from '../src/client/js/displayError.js';

describe('displayError should exist', () => {
	it('Test for the displayError function', () => {
		expect(displayError).toBeDefined();
	});
});
